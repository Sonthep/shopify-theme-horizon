
{% comment %}
  Commercial Kitchen Spare Parts - Landing Page Section
  Shopify Liquid Theme Template
{% endcomment %}

<style>
  :root {
    --color-primary: #0d7377;
    --color-primary-hover: #0a5c5f;
    --color-primary-light: #e6f4f4;
    --color-background: #fafaf9;
    --color-surface: #ffffff;
    --color-surface-alt: #f5f5f4;
    --color-text: #1c1917;
    --color-text-muted: #78716c;
    --color-border: #e7e5e4;
    --color-success: #22c55e;
    --color-warning: #f59e0b;
    --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    --radius: 12px;
    --radius-sm: 8px;
    --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
    --shadow: 0 4px 6px -1px rgba(0,0,0,0.1), 0 2px 4px -2px rgba(0,0,0,0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0,0,0,0.1), 0 4px 6px -4px rgba(0,0,0,0.1);
    --transition: all 0.2s ease;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  .landing-page {
    font-family: var(--font-sans);
    color: var(--color-text);
    background: var(--color-background);
    line-height: 1.6;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .section { padding: 80px 0; }
  .section--alt { background: var(--color-surface); }
  .section--dark { background: var(--color-text); color: var(--color-surface); }

  .section__header {
    text-align: center;
    margin-bottom: 48px;
  }

  .section__badge {
    display: inline-block;
    padding: 6px 16px;
    background: var(--color-primary-light);
    color: var(--color-primary);
    font-size: 13px;
    font-weight: 600;
    border-radius: 100px;
    margin-bottom: 16px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .section__title {
    font-size: clamp(28px, 4vw, 40px);
    font-weight: 700;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
    text-wrap: balance;
  }

  .section__subtitle {
    font-size: 18px;
    color: var(--color-text-muted);
    max-width: 600px;
    margin: 0 auto;
  }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 28px;
    font-size: 15px;
    font-weight: 600;
    border-radius: var(--radius);
    border: none;
    cursor: pointer;
    transition: var(--transition);
    text-decoration: none;
  }

  .btn--primary {
    background: var(--color-primary);
    color: white;
  }

  .btn--primary:hover {
    background: var(--color-primary-hover);
    transform: translateY(-1px);
  }

  .btn--secondary {
    background: var(--color-surface);
    color: var(--color-text);
    border: 1px solid var(--color-border);
  }

  .btn--secondary:hover {
    background: var(--color-surface-alt);
    border-color: var(--color-text-muted);
  }

  .btn--ghost {
    background: transparent;
    color: var(--color-primary);
  }

  .btn--ghost:hover { background: var(--color-primary-light); }

  /* ==================== HERO SECTION ==================== */
  .hero {
    padding: 100px 0 80px;
    background: linear-gradient(180deg, var(--color-surface) 0%, var(--color-background) 100%);
  }

  .hero__content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero__status {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 100px;
    font-size: 14px;
    margin-bottom: 24px;
  }

  .hero__status-dot {
    width: 8px;
    height: 8px;
    background: var(--color-success);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  .hero__title {
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin-bottom: 20px;
    text-wrap: balance;
  }

  .hero__title-highlight {
    color: var(--color-primary);
  }

  .hero__subtitle {
    font-size: 18px;
    color: var(--color-text-muted);
    margin-bottom: 32px;
    max-width: 560px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Search Box */
  .hero__search {
    max-width: 600px;
    margin: 0 auto 32px;
  }

  .search-box {
    position: relative;
    display: flex;
    background: var(--color-surface);
    border: 2px solid var(--color-border);
    border-radius: var(--radius);
    overflow: hidden;
    transition: var(--transition);
  }

  .search-box:focus-within {
    border-color: var(--color-primary);
    box-shadow: 0 0 0 4px var(--color-primary-light);
  }

  .search-box__icon {
    position: absolute;
    left: 16px;
    top: 50%;
    transform: translateY(-50%);
    color: var(--color-text-muted);
  }

  .search-box__input {
    flex: 1;
    padding: 16px 16px 16px 48px;
    border: none;
    outline: none;
    font-size: 16px;
    font-family: inherit;
    background: transparent;
  }

  .search-box__input::placeholder {
    color: var(--color-text-muted);
  }

  .search-box__btn {
    padding: 12px 24px;
    margin: 6px;
    background: var(--color-primary);
    color: white;
    border: none;
    border-radius: var(--radius-sm);
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition);
  }

  .search-box__btn:hover {
    background: var(--color-primary-hover);
  }

  /* Search Suggestions */
  .search-suggestions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 8px;
    margin-top: 16px;
  }

  .search-suggestions__item {
    padding: 6px 14px;
    background: var(--color-surface-alt);
    border-radius: 100px;
    font-size: 13px;
    color: var(--color-text-muted);
    cursor: pointer;
    transition: var(--transition);
    text-decoration: none;
  }

  .search-suggestions__item:hover {
    background: var(--color-primary-light);
    color: var(--color-primary);
  }

  /* Trust Badges */
  .hero__trust {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 32px;
    padding-top: 32px;
    border-top: 1px solid var(--color-border);
    max-width: 600px;
    margin: 0 auto;
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: var(--color-text-muted);
  }

  .trust-badge__icon {
    color: var(--color-primary);
  }

  /* ==================== CATEGORIES SECTION ==================== */
  .categories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px;
  }

  .category-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    padding: 24px;
    display: flex;
    align-items: center;
    gap: 16px;
    cursor: pointer;
    transition: var(--transition);
    text-decoration: none;
    color: inherit;
  }

  .category-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow);
    transform: translateY(-2px);
  }

  .category-card__icon {
    width: 56px;
    height: 56px;
    background: var(--color-primary-light);
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .category-card__content { flex: 1; }

  .category-card__title {
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  .category-card__count {
    font-size: 14px;
    color: var(--color-text-muted);
  }

  .category-card__arrow {
    color: var(--color-text-muted);
    transition: var(--transition);
  }

  .category-card:hover .category-card__arrow {
    color: var(--color-primary);
    transform: translateX(4px);
  }

  /* ==================== BRANDS SECTION ==================== */
  .brands-wrapper {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 12px;
  }

  .brand-chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 20px;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 100px;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: var(--transition);
    text-decoration: none;
    color: inherit;
  }

  .brand-chip:hover {
    border-color: var(--color-primary);
    background: var(--color-primary-light);
    color: var(--color-primary);
  }

  .brand-chip__logo {
    width: 24px;
    height: 24px;
    border-radius: 4px;
    object-fit: contain;
    background: var(--color-surface-alt);
  }

  /* ==================== PRODUCTS SECTION ==================== */
  .products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 24px;
  }

  .product-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    overflow: hidden;
    transition: var(--transition);
  }

  .product-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .product-card__image {
    position: relative;
    aspect-ratio: 1;
    background: var(--color-surface-alt);
    overflow: hidden;
  }

  .product-card__image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    padding: 16px;
    transition: transform 0.3s ease;
  }

  .product-card:hover .product-card__image img {
    transform: scale(1.05);
  }

  .product-card__badge {
    position: absolute;
    top: 12px;
    left: 12px;
    padding: 4px 10px;
    border-radius: 100px;
    font-size: 12px;
    font-weight: 600;
  }

  .product-card__badge--stock {
    background: #dcfce7;
    color: #166534;
  }

  .product-card__badge--low {
    background: #fef3c7;
    color: #92400e;
  }

  .product-card__body {
    padding: 20px;
  }

  .product-card__brand {
    font-size: 12px;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 4px;
  }

  .product-card__title {
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 8px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .product-card__sku {
    font-size: 13px;
    color: var(--color-text-muted);
    margin-bottom: 12px;
  }

  .product-card__footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .product-card__price {
    font-size: 18px;
    font-weight: 700;
    color: var(--color-primary);
  }

  .product-card__placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-surface-alt);
  }

  .product-card__btn {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: var(--transition);
    color: var(--color-text);
  }

  .product-card__btn:hover {
    background: var(--color-primary);
    border-color: var(--color-primary);
    color: white;
  }

  .products-more {
    text-align: center;
    margin-top: 40px;
  }

  /* ==================== PROBLEMS SECTION ==================== */
  .problems-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 24px;
  }

  .problem-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius);
    padding: 32px;
    transition: var(--transition);
  }

  .problem-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow);
  }

  .problem-card__icon {
    width: 48px;
    height: 48px;
    background: var(--color-primary-light);
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    margin-bottom: 20px;
  }

  .problem-card__question {
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .problem-card__answer {
    font-size: 15px;
    color: var(--color-text-muted);
    margin-bottom: 16px;
    line-height: 1.7;
  }

  .problem-card__link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--color-primary);
    font-weight: 600;
    font-size: 14px;
    text-decoration: none;
    transition: var(--transition);
  }

  .problem-card__link:hover {
    gap: 10px;
  }

  /* ==================== HOW IT WORKS SECTION ==================== */
  .steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 32px;
    position: relative;
  }

  .step {
    text-align: center;
    position: relative;
  }

  .step__number {
    width: 64px;
    height: 64px;
    background: var(--color-primary);
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    font-weight: 700;
    margin: 0 auto 20px;
  }

  .step__title {
    font-size: 20px;
    font-weight: 600;
    margin-bottom: 12px;
  }

  .step__description {
    font-size: 15px;
    color: var(--color-text-muted);
    max-width: 280px;
    margin: 0 auto;
  }

  /* ==================== CTA SECTION ==================== */
  .cta {
    text-align: center;
  }

  .cta .section__title {
    color: white;
  }

  .cta .section__subtitle {
    color: rgba(255,255,255,0.7);
    margin-bottom: 32px;
  }

  .cta__buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 16px;
  }

  .cta .btn--primary {
    background: white;
    color: var(--color-primary);
  }

  .cta .btn--primary:hover {
    background: var(--color-surface-alt);
  }

  .cta .btn--secondary {
    background: transparent;
    color: white;
    border-color: rgba(255,255,255,0.3);
  }

  .cta .btn--secondary:hover {
    background: rgba(255,255,255,0.1);
    border-color: rgba(255,255,255,0.5);
  }

  /* ==================== FOOTER ==================== */
  .footer {
    background: var(--color-surface);
    border-top: 1px solid var(--color-border);
    padding: 64px 0 32px;
  }

  .footer__grid {
    display: grid;
    grid-template-columns: 2fr repeat(3, 1fr);
    gap: 48px;
    margin-bottom: 48px;
  }

  .footer__brand-description {
    font-size: 14px;
    color: var(--color-text-muted);
    margin: 16px 0 24px;
    max-width: 280px;
  }

  .footer__contact {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .footer__contact-item {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    color: var(--color-text-muted);
  }

  .footer__title {
    font-size: 14px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 20px;
  }

  .footer__links {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .footer__links a {
    font-size: 14px;
    color: var(--color-text-muted);
    text-decoration: none;
    transition: var(--transition);
  }

  .footer__links a:hover {
    color: var(--color-primary);
  }

  .footer__bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    padding-top: 32px;
    border-top: 1px solid var(--color-border);
    font-size: 14px;
    color: var(--color-text-muted);
  }

  .footer__payments {
    display: flex;
    gap: 12px;
  }

  .footer__payment-icon {
    width: 40px;
    height: 24px;
    background: var(--color-surface-alt);
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    font-weight: 600;
    color: var(--color-text-muted);
  }

  /* ==================== FLOATING CHAT ==================== */
  .floating-chat {
    position: fixed;
    bottom: 24px;
    right: 24px;
    z-index: 1000;
  }

  .floating-chat__btn {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: var(--color-primary);
    color: white;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
    transition: var(--transition);
  }

  .floating-chat__btn:hover {
    background: var(--color-primary-hover);
    transform: scale(1.05);
  }

  /* ==================== RESPONSIVE ==================== */
  @media (max-width: 768px) {
    .section { padding: 60px 0; }
    .container { padding: 0 16px; }
    
    .hero { padding: 60px 0; }
    .hero__trust { flex-direction: column; gap: 16px; }
    
    .footer__grid {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    
    .footer__bottom {
      flex-direction: column;
      text-align: center;
    }
    
    .search-box {
      flex-direction: column;
    }
    
    .search-box__btn {
      margin: 0 6px 6px;
    }
  }
</style>

<div class="landing-page">
  <!-- Hero Section -->
  <section class="hero">
    <div class="container">
      <div class="hero__content">
        <div class="hero__status">
          <span class="hero__status-dot"></span>
          <span>{{ section.settings.status_text | default: '10,000+ Parts in Stock' }}</span>
        </div>
        
        <h1 class="hero__title">
          {{ section.settings.hero_title_1 | default: 'Commercial Kitchen' }}
          <span class="hero__title-highlight">{{ section.settings.hero_title_2 | default: 'Spare Parts' }}</span>
        </h1>
        
        <p class="hero__subtitle">
          {{ section.settings.hero_subtitle | default: 'Find OEM-compatible parts for your commercial kitchen equipment. Fast delivery, expert support, and competitive pricing.' }}
        </p>
        
        <div class="hero__search">
          <form action="{{ routes.search_url }}" method="get" class="search-box">
            <svg class="search-box__icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="11" cy="11" r="8"/>
              <path d="m21 21-4.3-4.3"/>
            </svg>
            <input type="text" name="q" class="search-box__input" placeholder="{{ section.settings.search_placeholder | default: 'Search by part number, model, or keyword...' }}">
            <button type="submit" class="search-box__btn">Search</button>
          </form>
          
          <div class="search-suggestions">
            {% for block in section.blocks %}
              {% if block.type == 'search_suggestion' %}
                <a href="{{ block.settings.link }}" class="search-suggestions__item">{{ block.settings.text }}</a>
              {% endif %}
            {% endfor %}
            {% if section.blocks.size == 0 %}
              <a href="{{ routes.search_url }}?q=thermostat" class="search-suggestions__item">Thermostat</a>
              <a href="{{ routes.search_url }}?q=heating+element" class="search-suggestions__item">Heating Element</a>
              <a href="{{ routes.search_url }}?q=door+gasket" class="search-suggestions__item">Door Gasket</a>
              <a href="{{ routes.search_url }}?q=control+board" class="search-suggestions__item">Control Board</a>
            {% endif %}
          </div>
        </div>
        
        <div class="hero__trust">
          <div class="trust-badge">
            <svg class="trust-badge__icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10"/>
              <path d="m9 12 2 2 4-4"/>
            </svg>
            <span>OEM Compatible</span>
          </div>
          <div class="trust-badge">
            <svg class="trust-badge__icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M13 2 3 14h9l-1 8 10-12h-9l1-8z"/>
            </svg>
            <span>Same-Day Dispatch</span>
          </div>
          <div class="trust-badge">
            <svg class="trust-badge__icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92"/>
            </svg>
            <span>Expert Support</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Categories Section -->
  <section class="section section--alt">
    <div class="container">
      <div class="section__header">
        <span class="section__badge">Categories</span>
        <h2 class="section__title">{{ section.settings.categories_title | default: 'Browse by Equipment Type' }}</h2>
        <p class="section__subtitle">{{ section.settings.categories_subtitle | default: 'Find the right parts for your specific equipment' }}</p>
      </div>
      
      <div class="categories-grid">
        {% for collection in collections limit: 6 %}
          <a href="{{ collection.url }}" class="category-card">
            <div class="category-card__icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect width="18" height="18" x="3" y="3" rx="2"/>
                <path d="M3 9h18"/>
                <path d="M9 21V9"/>
              </svg>
            </div>
            <div class="category-card__content">
              <h3 class="category-card__title">{{ collection.title }}</h3>
              <p class="category-card__count">{{ collection.products_count }} parts</p>
            </div>
            <svg class="category-card__arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14"/>
              <path d="m12 5 7 7-7 7"/>
            </svg>
          </a>
        {% else %}
          {% assign category_names = 'Ovens & Ranges,Refrigeration,Fryers,Dishwashers,Food Prep,Ice Machines' | split: ',' %}
          {% assign category_counts = '2,450,1,890,1,234,987,756,543' | split: ',' %}
          {% for name in category_names %}
            <a href="{{ routes.all_products_collection_url }}" class="category-card">
              <div class="category-card__icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <rect width="18" height="18" x="3" y="3" rx="2"/>
                  <path d="M3 9h18"/>
                  <path d="M9 21V9"/>
                </svg>
              </div>
              <div class="category-card__content">
                <h3 class="category-card__title">{{ name }}</h3>
                <p class="category-card__count">{{ category_counts[forloop.index0] }} parts</p>
              </div>
              <svg class="category-card__arrow" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14"/>
                <path d="m12 5 7 7-7 7"/>
              </svg>
            </a>
          {% endfor %}
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Brands Section -->
  <section class="section">
    <div class="container">
      <div class="section__header">
        <span class="section__badge">Trusted Brands</span>
        <h2 class="section__title">{{ section.settings.brands_title | default: 'Shop by Brand' }}</h2>
        <p class="section__subtitle">{{ section.settings.brands_subtitle | default: 'OEM and compatible parts from leading manufacturers' }}</p>
      </div>
      
      <div class="brands-wrapper">
        {% assign brand_names = 'Robot Coupe,Rational,Hobart,True,Hoshizaki,Vulcan,Garland,Manitowoc,Alto-Shaam,Bunn' | split: ',' %}
        {% for brand in brand_names %}
          <a href="{{ routes.collections_url }}/{{ brand | handleize }}" class="brand-chip">
            <span class="brand-chip__logo">{{ brand | slice: 0, 2 | upcase }}</span>
            <span>{{ brand }}</span>
          </a>
        {% endfor %}
      </div>
    </div>
  </section>

  <!-- Featured Products Section -->
  <section class="section section--alt">
    <div class="container">
      <div class="section__header">
        <span class="section__badge">Featured</span>
        <h2 class="section__title">{{ section.settings.products_title | default: 'Popular Parts' }}</h2>
        <p class="section__subtitle">{{ section.settings.products_subtitle | default: 'Most ordered parts this month' }}</p>
      </div>
      
      <div class="products-grid">
        {% for product in collections.all.products limit: 8 %}
          <div class="product-card">
            <div class="product-card__image">
              {% if product.available %}
                <span class="product-card__badge product-card__badge--stock">In Stock</span>
              {% else %}
                <span class="product-card__badge product-card__badge--low">Low Stock</span>
              {% endif %}
              {% if product.featured_image %}
                <img src="{{ product.featured_image | image_url: width: 400 }}" alt="{{ product.title }}" loading="lazy" width="400" height="400">
              {% else %}
                <div class="product-card__placeholder" aria-hidden="true">
                  <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" opacity="0.3"><rect width="18" height="18" x="3" y="3" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
                </div>
              {% endif %}
            </div>
            <div class="product-card__body">
              <p class="product-card__brand">{{ product.vendor }}</p>
              <h3 class="product-card__title">{{ product.title }}</h3>
              <p class="product-card__sku">SKU: {{ product.variants.first.sku | default: product.id }}</p>
              <div class="product-card__footer">
                <span class="product-card__price">{{ product.price | money }}</span>
                <form action="{{ routes.cart_add_url }}" method="post">
                  <input type="hidden" name="id" value="{{ product.variants.first.id }}">
                  <button type="submit" class="product-card__btn" aria-label="Add to cart">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <circle cx="8" cy="21" r="1"/>
                      <circle cx="19" cy="21" r="1"/>
                      <path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"/>
                    </svg>
                  </button>
                </form>
              </div>
            </div>
          </div>
        {% else %}
          {% assign sample_products = 'Thermostat Assembly,Heating Element,Door Gasket Seal,Control Board PCB,Fan Motor Assembly,Ignition Module,Water Inlet Valve,Compressor Start Relay' | split: ',' %}
          {% assign sample_brands = 'Rational,Hobart,True,Robot Coupe,Hoshizaki,Vulcan,Manitowoc,Alto-Shaam' | split: ',' %}
          {% assign sample_prices = '$89.99,$124.50,$45.00,$289.00,$156.75,$67.25,$78.90,$195.00' | split: ',' %}
          {% for name in sample_products %}
            <div class="product-card">
              <div class="product-card__image">
                {% assign stock_mod = forloop.index | modulo: 3 %}
                {% if stock_mod == 0 %}
                  <span class="product-card__badge product-card__badge--low">Low Stock</span>
                {% else %}
                  <span class="product-card__badge product-card__badge--stock">In Stock</span>
                {% endif %}
                <div class="product-card__placeholder" aria-hidden="true">
                  <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" opacity="0.3"><rect width="18" height="18" x="3" y="3" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
                </div>
              </div>
              <div class="product-card__body">
                <p class="product-card__brand">{{ sample_brands[forloop.index0] }}</p>
                <h3 class="product-card__title">{{ name }}</h3>
                <p class="product-card__sku">SKU: SKP-{{ forloop.index }}0{{ forloop.index }}45</p>
                <div class="product-card__footer">
                  <span class="product-card__price">{{ sample_prices[forloop.index0] }}</span>
                  <button type="button" class="product-card__btn" aria-label="Add to cart">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                      <circle cx="8" cy="21" r="1"/>
                      <circle cx="19" cy="21" r="1"/>
                      <path d="M2.05 2.05h2l2.66 12.42a2 2 0 0 0 2 1.58h9.78a2 2 0 0 0 1.95-1.57l1.65-7.43H5.12"/>
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          {% endfor %}
        {% endfor %}
      </div>
      
      <div class="products-more">
        <a href="{{ routes.all_products_collection_url }}" class="btn btn--secondary">
          View All Products
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14"/>
            <path d="m12 5 7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Problems Section -->
  <section class="section">
    <div class="container">
      <div class="section__header">
        <span class="section__badge">Solutions</span>
        <h2 class="section__title">{{ section.settings.problems_title | default: 'Common Problems We Solve' }}</h2>
        <p class="section__subtitle">{{ section.settings.problems_subtitle | default: 'Quick fixes for frequent equipment issues' }}</p>
      </div>
      
      <div class="problems-grid">
        <div class="problem-card">
          <div class="problem-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2v4"/>
              <path d="m6.8 14-3.5 2"/>
              <path d="m20.7 16-3.5-2"/>
              <path d="M6.8 10 3.3 8"/>
              <path d="m20.7 8-3.5 2"/>
              <path d="m9 22 3-8 3 8"/>
              <path d="M8 22h8"/>
              <path d="M12 6a4 4 0 0 0-4 4c0 2.2 1.8 4 4 4s4-1.8 4-4a4 4 0 0 0-4-4"/>
            </svg>
          </div>
          <h3 class="problem-card__question">Oven not heating properly?</h3>
          <p class="problem-card__answer">Usually caused by a faulty thermostat or heating element. We stock replacements for all major brands with same-day dispatch.</p>
          <a href="{{ routes.collections_url }}/heating-elements" class="problem-card__link">
            Shop Heating Parts
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14"/>
              <path d="m12 5 7 7-7 7"/>
            </svg>
          </a>
        </div>
        
        <div class="problem-card">
          <div class="problem-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 22a10 10 0 1 1 0-20 10 10 0 0 1 0 20z"/>
              <path d="M12 6v6l4 2"/>
            </svg>
          </div>
          <h3 class="problem-card__question">Refrigerator not cooling?</h3>
          <p class="problem-card__answer">Check the compressor, condenser fan, or thermostat. Our diagnostic guides help you identify the issue quickly.</p>
          <a href="{{ routes.collections_url }}/refrigeration" class="problem-card__link">
            Shop Refrigeration Parts
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14"/>
              <path d="m12 5 7 7-7 7"/>
            </svg>
          </a>
        </div>
        
        <div class="problem-card">
          <div class="problem-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M14 4v10.54a4 4 0 1 1-4 0V4a2 2 0 0 1 4 0Z"/>
            </svg>
          </div>
          <h3 class="problem-card__question">Door seal leaking?</h3>
          <p class="problem-card__answer">Worn gaskets cause energy loss and temperature issues. Find exact-fit replacements for ovens, fridges, and dishwashers.</p>
          <a href="{{ routes.collections_url }}/gaskets" class="problem-card__link">
            Shop Door Gaskets
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14"/>
              <path d="m12 5 7 7-7 7"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works Section -->
  <section class="section section--alt">
    <div class="container">
      <div class="section__header">
        <span class="section__badge">Process</span>
        <h2 class="section__title">{{ section.settings.how_title | default: 'How It Works' }}</h2>
        <p class="section__subtitle">{{ section.settings.how_subtitle | default: 'Get your equipment running in 3 simple steps' }}</p>
      </div>
      
      <div class="steps">
        <div class="step">
          <div class="step__number">1</div>
          <h3 class="step__title">Find Your Part</h3>
          <p class="step__description">Search by part number, model, or equipment type. Use our compatibility checker to ensure perfect fit.</p>
        </div>
        
        <div class="step">
          <div class="step__number">2</div>
          <h3 class="step__title">Fast Checkout</h3>
          <p class="step__description">Secure payment with multiple options. Business accounts available for volume discounts.</p>
        </div>
        
        <div class="step">
          <div class="step__number">3</div>
          <h3 class="step__title">Quick Delivery</h3>
          <p class="step__description">Same-day dispatch on orders before 2pm. Track your order in real-time until delivery.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="section section--dark cta">
    <div class="container">
      <div class="section__header">
        <h2 class="section__title">{{ section.settings.cta_title | default: 'Ready to Get Your Kitchen Running?' }}</h2>
        <p class="section__subtitle">{{ section.settings.cta_subtitle | default: 'Join thousands of restaurants and food service businesses who trust us for their spare parts needs.' }}</p>
      </div>
      
      <div class="cta__buttons">
        <a href="{{ routes.all_products_collection_url }}" class="btn btn--primary">
          Browse All Parts
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 12h14"/>
            <path d="m12 5 7 7-7 7"/>
          </svg>
        </a>
        <a href="/pages/contact" class="btn btn--secondary">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
          </svg>
          Contact Sales
        </a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer__grid">
        <div class="footer__brand">
          <h3 style="font-size: 20px; font-weight: 700;">{{ shop.name }}</h3>
          <p class="footer__brand-description">Your trusted source for commercial kitchen spare parts. Quality parts, fast delivery, expert support.</p>
          <div class="footer__contact">
            <div class="footer__contact-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
              </svg>
              <span>{{ section.settings.phone | default: '1-800-PARTS-24' }}</span>
            </div>
            <div class="footer__contact-item">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <rect width="20" height="16" x="2" y="4" rx="2"/>
                <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
              </svg>
              <span>{{ section.settings.email | default: 'support@kitchenparts.com' }}</span>
            </div>
          </div>
        </div>
        
        <div class="footer__column">
          <h4 class="footer__title">Products</h4>
          <ul class="footer__links">
            <li><a href="{{ routes.collections_url }}/ovens">Ovens & Ranges</a></li>
            <li><a href="{{ routes.collections_url }}/refrigeration">Refrigeration</a></li>
            <li><a href="{{ routes.collections_url }}/fryers">Fryers</a></li>
            <li><a href="{{ routes.collections_url }}/dishwashers">Dishwashers</a></li>
            <li><a href="{{ routes.all_products_collection_url }}">All Products</a></li>
          </ul>
        </div>
        
        <div class="footer__column">
          <h4 class="footer__title">Support</h4>
          <ul class="footer__links">
            <li><a href="/pages/contact">Contact Us</a></li>
            <li><a href="/pages/faq">FAQs</a></li>
            <li><a href="/pages/shipping">Shipping Info</a></li>
            <li><a href="/pages/returns">Returns</a></li>
            <li><a href="/pages/warranty">Warranty</a></li>
          </ul>
        </div>
        
        <div class="footer__column">
          <h4 class="footer__title">Company</h4>
          <ul class="footer__links">
            <li><a href="/pages/about">About Us</a></li>
            <li><a href="/blogs/news">Blog</a></li>
            <li><a href="/pages/careers">Careers</a></li>
            <li><a href="/pages/privacy">Privacy Policy</a></li>
            <li><a href="/pages/terms">Terms of Service</a></li>
          </ul>
        </div>
      </div>
      
      <div class="footer__bottom">
        <p>&copy; {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.</p>
        <div class="footer__payments">
          <span class="footer__payment-icon">VISA</span>
          <span class="footer__payment-icon">MC</span>
          <span class="footer__payment-icon">AMEX</span>
          <span class="footer__payment-icon">PP</span>
        </div>
      </div>
    </div>
  </footer>

  <!-- Floating Chat Button -->
  <div class="floating-chat">
    <button class="floating-chat__btn" aria-label="Open chat" onclick="window.Shopify && window.Shopify.Messenger ? Shopify.Messenger.open() : alert('Chat support available during business hours')">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="m3 21 1.9-5.7a8.5 8.5 0 1 1 3.8 3.8z"/>
      </svg>
    </button>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Search suggestions click handler
    const suggestions = document.querySelectorAll('.search-suggestions__item');
    const searchInput = document.querySelector('.search-box__input');
    
    suggestions.forEach(function(suggestion) {
      suggestion.addEventListener('click', function(e) {
        if (searchInput) {
          e.preventDefault();
          searchInput.value = this.textContent.trim();
          searchInput.focus();
        }
      });
    });

    // Product card add to cart animation
    const addToCartBtns = document.querySelectorAll('.product-card__btn');
    
    addToCartBtns.forEach(function(btn) {
      btn.addEventListener('click', function() {
        const originalHTML = this.innerHTML;
        this.innerHTML = '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>';
        this.style.background = 'var(--color-success)';
        this.style.borderColor = 'var(--color-success)';
        this.style.color = 'white';
        
        setTimeout(function() {
          btn.innerHTML = originalHTML;
          btn.style.background = '';
          btn.style.borderColor = '';
          btn.style.color = '';
        }, 1500);
      });
    });

    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(function(anchor) {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  });
</script>

{% schema %}
{
  "name": "Landing Page",
  "tag": "section",
  "class": "landing-page-section",
  "settings": [
    {
      "type": "header",
      "content": "Hero Section"
    },
    {
      "type": "text",
      "id": "status_text",
      "label": "Status Badge Text",
      "default": "10,000+ Parts in Stock"
    },
    {
      "type": "text",
      "id": "hero_title_1",
      "label": "Hero Title Line 1",
      "default": "Commercial Kitchen"
    },
    {
      "type": "text",
      "id": "hero_title_2",
      "label": "Hero Title Line 2 (Highlighted)",
      "default": "Spare Parts"
    },
    {
      "type": "textarea",
      "id": "hero_subtitle",
      "label": "Hero Subtitle",
      "default": "Find OEM-compatible parts for your commercial kitchen equipment. Fast delivery, expert support, and competitive pricing."
    },
    {
      "type": "text",
      "id": "search_placeholder",
      "label": "Search Placeholder",
      "default": "Search by part number, model, or keyword..."
    },
    {
      "type": "header",
      "content": "Categories Section"
    },
    {
      "type": "text",
      "id": "categories_title",
      "label": "Categories Title",
      "default": "Browse by Equipment Type"
    },
    {
      "type": "textarea",
      "id": "categories_subtitle",
      "label": "Categories Subtitle",
      "default": "Find the right parts for your specific equipment"
    },
    {
      "type": "header",
      "content": "Brands Section"
    },
    {
      "type": "text",
      "id": "brands_title",
      "label": "Brands Title",
      "default": "Shop by Brand"
    },
    {
      "type": "textarea",
      "id": "brands_subtitle",
      "label": "Brands Subtitle",
      "default": "OEM and compatible parts from leading manufacturers"
    },
    {
      "type": "header",
      "content": "Products Section"
    },
    {
      "type": "text",
      "id": "products_title",
      "label": "Products Title",
      "default": "Popular Parts"
    },
    {
      "type": "textarea",
      "id": "products_subtitle",
      "label": "Products Subtitle",
      "default": "Most ordered parts this month"
    },
    {
      "type": "header",
      "content": "Problems Section"
    },
    {
      "type": "text",
      "id": "problems_title",
      "label": "Problems Title",
      "default": "Common Problems We Solve"
    },
    {
      "type": "textarea",
      "id": "problems_subtitle",
      "label": "Problems Subtitle",
      "default": "Quick fixes for frequent equipment issues"
    },
    {
      "type": "header",
      "content": "How It Works Section"
    },
    {
      "type": "text",
      "id": "how_title",
      "label": "How It Works Title",
      "default": "How It Works"
    },
    {
      "type": "textarea",
      "id": "how_subtitle",
      "label": "How It Works Subtitle",
      "default": "Get your equipment running in 3 simple steps"
    },
    {
      "type": "header",
      "content": "CTA Section"
    },
    {
      "type": "text",
      "id": "cta_title",
      "label": "CTA Title",
      "default": "Ready to Get Your Kitchen Running?"
    },
    {
      "type": "textarea",
      "id": "cta_subtitle",
      "label": "CTA Subtitle",
      "default": "Join thousands of restaurants and food service businesses who trust us for their spare parts needs."
    },
    {
      "type": "header",
      "content": "Footer Contact"
    },
    {
      "type": "text",
      "id": "phone",
      "label": "Phone Number",
      "default": "1-800-PARTS-24"
    },
    {
      "type": "text",
      "id": "email",
      "label": "Email Address",
      "default": "support@kitchenparts.com"
    }
  ],
  "blocks": [
    {
      "type": "search_suggestion",
      "name": "Search Suggestion",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Suggestion Text",
          "default": "Thermostat"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Landing Page",
      "blocks": [
        { "type": "search_suggestion", "settings": { "text": "Thermostat" } },
        { "type": "search_suggestion", "settings": { "text": "Heating Element" } },
        { "type": "search_suggestion", "settings": { "text": "Door Gasket" } },
        { "type": "search_suggestion", "settings": { "text": "Control Board" } }
      ]
    }
  ]
}
{% endschema %}
No Database Connected
Connect a database like Supabase or Upstash to view, search and manage your data, all in v0
Commercial kitchen spare parts - v0 by Vercel